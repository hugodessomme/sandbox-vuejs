/**
 * 1. Rename this file to config.local.js
 * 2. Add your configuration in the config object
 * 3. Use import statement in your JavaScript file to access your config data
 */

const config = {
    currency_converter_api_key: "YOUR_API_KEY",
};

export default config;